---
AWSTemplateFormatVersion: "2010-09-09"

Description: >-
  CloudFormation bootstrap for the Agentic Runtime Security on AWS workshop.
  Provisions tier-1 (EKS foundation, ~17 min) AND tier-2 (Vault + IVIA, ~15 min)
  via CodeBuild before the attendee session opens, so that the three licensing
  secrets are supplied once by the organizer instead of being handed to every
  attendee. Attendees run tier-3 themselves as the hands-on lesson.
  Source pattern: CodeBuild sources workshop/assets/ from S3 (TerraformSourceBucket) —
  no git clone, no public mirror. Template location: workshop/assets/cfn/bootstrap.yaml

Parameters:
  AcmeEmail:
    Type: String
    NoEcho: true
    Default: ""
    Description: >-
      Let's Encrypt registration email for TLS certificate issuance (ACME).
      OPTIONAL — leave blank to register a no-contact ACME account (Let's Encrypt
      turned off expiration-notice emails and deleted stored account addresses on
      2025-06-04). The event organizer MAY set a real, reachable address (not
      *@example.com) if renewal/revocation notices are wanted.

  # ─── Tier-2 licensing secrets ───────────────────────────────────────────────
  # The three non-committable workshop inputs. Pre-provisioning tier 2 exists so
  # that the organizer supplies them ONCE here instead of handing them to every
  # attendee individually (#21). All three are NoEcho, are stored in Secrets
  # Manager under a dedicated CMK the attendee role is explicitly denied on, and
  # reach CodeBuild as SECRETS_MANAGER-typed environment variables — never as the
  # PLAINTEXT type, whose values are returned verbatim by batch-get-projects.
  IcrEntitlementKey:
    Type: String
    NoEcho: true
    MinLength: 1
    Description: >-
      REQUIRED. IBM Container Registry entitlement key. Tier 2 builds the IVIA
      image-pull secret from it (cp.icr.io); without a valid key every IVIA pod
      lands in ImagePullBackOff. Obtain from the IBM Container Library.

  VaultEnterpriseLicense:
    Type: String
    NoEcho: true
    MinLength: 1
    Description: >-
      REQUIRED. Full contents of the Vault Enterprise .hclic license file (the
      license string itself, not a path). Vault Enterprise will not start
      without it, and the workshop needs the platform-standard / agentic-iam
      features it carries.

  IviaMmfaPushClientSecret:
    Type: String
    NoEcho: true
    MinLength: 1
    Description: >-
      REQUIRED. IBM Verify MMFA push credential, used by Use Case 3's CIBA
      mobile-push flow. Although tier 2's Terraform guards this on a count and
      tolerates an empty value, deploy-workshop.sh's preflight does not — it
      prompts for it and refuses an empty answer, so an unattended build must
      have it. Required here to match that contract rather than fail 20 minutes
      into the build.

  TerraformSourceBucket:
    Type: String
    Description: >-
      S3 bucket holding the uploaded workshop/assets/ tree (buildspec/ + terraform/).
      In Workshop Studio this is the per-workshop assets bucket that `aws s3 sync
      ./assets` populates; for the dev-account sim it is a bucket you create and upload
      to. CodeBuild sources buildspec/buildspec.yml and terraform/ from here.

  AssetsKeyPrefix:
    Type: String
    Default: ""
    Description: >-
      Key prefix (with trailing slash) under TerraformSourceBucket where the assets
      live — e.g. "agentic-runtime-security-aws/" in Workshop Studio. Leave empty when
      assets are uploaded to the bucket root (dev-account sim).

  AttendeeRoleName:
    Type: String
    Default: WSParticipantRole
    Description: >-
      IAM role NAME (not ARN) of the attendee identity that must receive EKS
      cluster-admin after tier-1 is provisioned by CodeBuild. In Workshop Studio the
      attendee assumes WSParticipantRole; override only if a real WS event exposes a
      differently-named participant role (verify-on-real-WS). The buildspec builds the
      full ARN as arn:aws:iam::<account-id>:role/<AttendeeRoleName> with the account id
      resolved dynamically via STS — never hardcoded.

  ImageSource:
    Type: String
    Default: ecr
    AllowedValues:
      - ecr
    Description: >-
      How the Use Case container images are sourced at-an-event. ECR is the
      supported at-an-event path: CodeBuild builds the images from source and pushes
      them to the attendee's own ECR (requires PrivilegedMode). The no-build GHCR
      opt-out is a self-paced, bring-your-own-images path only (see the repo README)
      and is not offered at an event. Flows to the buildspec as the IMAGE_SOURCE env
      var and on to bootstrap.sh / deploy-workshop.sh --image-source.

Resources:

  # ─── S3: tier-1 state staging bucket ───────────────────────────────────────
  # Tier-1 Terraform state is staged here after the CodeBuild run so attendees
  # can pull it with `aws s3 cp` at the start of their tier-2/3 session (D-04).
  # Threat T-11-01: SSE-AES256 + public-access block + versioning (no delete on CFN Delete — D-04).
  StateBucket:
    Type: "AWS::S3::Bucket"
    Properties:
      BucketEncryption:
        ServerSideEncryptionConfiguration:
          - ServerSideEncryptionByDefault:
              SSEAlgorithm: AES256
      PublicAccessBlockConfiguration:
        BlockPublicAcls: true
        BlockPublicPolicy: true
        IgnorePublicAcls: true
        RestrictPublicBuckets: true
      VersioningConfiguration:
        Status: Enabled

  # ─── S3: keep attendees out of the full tier-2 state ───────────────────────
  # Terraform records every managed resource's attributes in state, so the tier-2
  # state file contains the Vault license, the ICR entitlement key (inside the
  # dockerconfigjson pull secret) and the MMFA credential in plaintext. The Delete
  # path needs that file to run `terraform destroy`, so it is staged here rather
  # than discarded — under tier2-private/, which this policy closes off.
  #
  # The attendee role carries ReadOnlyAccess, which grants s3:Get*/List* on every
  # bucket in the account; a prefix is not private by naming alone. Attendees read
  # tier1/ and tier2/ (the sanitized outputs-only copy) as normal.
  StateBucketPolicy:
    Type: "AWS::S3::BucketPolicy"
    Properties:
      Bucket: !Ref StateBucket
      PolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Sid: DenyAttendeeAccessToFullTier2State
            Effect: Deny
            Principal: "*"
            Action: "s3:*"
            Resource: !Sub "${StateBucket.Arn}/tier2-private/*"
            Condition:
              ArnEquals:
                "aws:PrincipalArn": !Sub "arn:aws:iam::${AWS::AccountId}:role/${AttendeeRoleName}"

  # ─── IAM: CodeBuild execution role ──────────────────────────────────────────
  # INITIAL non-Admin role scoped to tier-1 service namespaces + IAM management
  # (tier-1 Terraform creates 10+ IAM roles; PowerUserAccess alone is insufficient).
  # Threat T-11-03: role is non-Admin (no wildcard-on-star grant). Action set is
  # finalized in plan 11-04 via aws accessanalyzer against a live CloudTrail run.
  CodeBuildServiceRole:
    Type: "AWS::IAM::Role"
    Properties:
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service: codebuild.amazonaws.com
            Action: "sts:AssumeRole"
      Policies:
        - PolicyName: Tier1DeployPermissions
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Sid: Tier1ServiceNamespaces
                Effect: Allow
                Action:
                  - "eks:*"
                  - "ec2:*"
                  - "ecr:*"
                  # tier-1 creates nested CloudFormation stacks via Terraform's
                  # aws_cloudformation_stack (bedrock_kb_index AOSS index +
                  # eks_blueprints_addons usage_telemetry) — CreateStack/DescribeStacks/etc.
                  - "cloudformation:*"
                  - "rds:*"
                  - "s3:*"
                  - "bedrock:*"
                  - "aoss:*"
                  - "logs:*"
                  - "cloudwatch:*"
                  - "firehose:*"
                  - "kms:*"
                  - "acm:*"
                  - "route53:*"
                  - "elasticloadbalancing:*"
                  - "cloudtrail:*"
                  - "athena:*"
                  - "dynamodb:*"
                  - "lambda:*"
                  - "sts:*"
                  - "secretsmanager:*"
                  - "ssm:*"
                  - "ssmmessages:*"
                  - "events:*"
                  - "glue:*"
                  - "opensearch:*"
                  - "aoss:*"
                  - "wafv2:*"
                  - "xray:*"
                  - "tag:*"
                Resource: "*"
              - Sid: Tier1IamManagement
                # Tier-1 Terraform creates IAM roles (fluent-bit, firehose, vault-kms,
                # bedrock-kb, IRSA roles, etc.). IAM management actions are required.
                # PowerUserAccess denies all iam:* — this statement bridges that gap.
                # Scoped to management CRUD actions only; NOT iam:* (which would be Admin).
                Effect: Allow
                Action:
                  - "iam:Create*"
                  - "iam:Attach*"
                  - "iam:Detach*"
                  - "iam:Delete*"
                  - "iam:Get*"
                  - "iam:List*"
                  - "iam:Put*"
                  - "iam:Tag*"
                  - "iam:Untag*"
                  - "iam:Update*"
                  - "iam:PassRole"
                  - "iam:AddRoleToInstanceProfile"
                  - "iam:RemoveRoleFromInstanceProfile"
                Resource: "*"

  # ─── KMS: dedicated CMK for the organizer's licensing secrets ──────────────
  # Deliberately NOT the workshop CMK in modules/audit: that key's policy opens
  # kms:ViaService secretsmanager to attendees on purpose, so anything encrypted
  # with it is attendee-readable. These three secrets must not be.
  #
  # The attendee role carries SecretsManagerReadWrite, which is enough to call
  # GetSecretValue on any secret in the account. What stops it here is the key:
  # without kms:Decrypt on this CMK the call fails. The explicit Deny is keyed on
  # aws:PrincipalArn rather than naming the role as a Principal, because KMS
  # validates policy principals exist and WSParticipantRole is absent in a
  # dev-account simulation.
  BuildSecretsKey:
    Type: "AWS::KMS::Key"
    Properties:
      Description: !Sub "${AWS::StackName} — encrypts the organizer-supplied tier-2 licensing secrets"
      EnableKeyRotation: true
      KeyPolicy:
        Version: "2012-10-17"
        Statement:
          # Required base statement: without it the key is unmanageable and
          # CloudFormation cannot create/encrypt the secrets or delete the key.
          - Sid: EnableIAMPoliciesForAccountAdministration
            Effect: Allow
            Principal:
              AWS: !Sub "arn:aws:iam::${AWS::AccountId}:root"
            Action: "kms:*"
            Resource: "*"
          # Decrypt to read the secrets at build start; Encrypt/GenerateDataKey
          # because the build also writes the full tier-2 state to S3 under
          # SSE-KMS with this key.
          - Sid: AllowCodeBuildRoleToUseTheKey
            Effect: Allow
            Principal:
              AWS: !GetAtt CodeBuildServiceRole.Arn
            Action:
              - "kms:Decrypt"
              - "kms:DescribeKey"
              - "kms:Encrypt"
              - "kms:GenerateDataKey*"
              - "kms:ReEncrypt*"
            Resource: "*"
          # Explicit Deny beats every Allow, including the account-root statement
          # above and any IAM policy attached to the attendee role.
          - Sid: DenyAttendeeRole
            Effect: Deny
            Principal: "*"
            Action: "kms:*"
            Resource: "*"
            Condition:
              ArnEquals:
                "aws:PrincipalArn": !Sub "arn:aws:iam::${AWS::AccountId}:role/${AttendeeRoleName}"

  # ─── Secrets Manager: the three tier-2 licensing secrets ───────────────────
  # These live for the lifetime of the stack rather than being deleted at the end
  # of the create build: the stack's Delete path runs `terraform destroy` against
  # the tier-2 root, which needs these variables defined. Confidentiality comes
  # from BuildSecretsKey's Deny, not from deletion timing. They are removed with
  # the stack.
  IcrEntitlementKeySecret:
    Type: "AWS::SecretsManager::Secret"
    Properties:
      Name: !Sub "${AWS::StackName}-icr-entitlement-key"
      Description: "IBM Container Registry entitlement key — tier-2 IVIA image pull"
      KmsKeyId: !Ref BuildSecretsKey
      SecretString: !Ref IcrEntitlementKey

  VaultEnterpriseLicenseSecret:
    Type: "AWS::SecretsManager::Secret"
    Properties:
      Name: !Sub "${AWS::StackName}-vault-enterprise-license"
      Description: "Vault Enterprise .hclic contents — tier-2 vault_server"
      KmsKeyId: !Ref BuildSecretsKey
      SecretString: !Ref VaultEnterpriseLicense

  IviaMmfaPushSecret:
    Type: "AWS::SecretsManager::Secret"
    Properties:
      Name: !Sub "${AWS::StackName}-ivia-mmfa-push-client-secret"
      Description: "IBM Verify MMFA push credential — tier-2 IVIA (optional)"
      KmsKeyId: !Ref BuildSecretsKey
      SecretString: !Ref IviaMmfaPushClientSecret

  # ─── CodeBuild: tier-1 deploy project ──────────────────────────────────────
  # Source.Type=S3 (folder) — CodeBuild downloads the uploaded workshop/assets/ tree
  # from TerraformSourceBucket into $CODEBUILD_SRC_DIR. BuildSpec is read from
  # buildspec/buildspec.yml within that tree; the infrastructure/ tree lives under
  # terraform/. No OAuth, no git clone. (T-11 source-location decision)
  # Environment variables are injected from CFN parameters. ACME_EMAIL passes the
  # NoEcho parameter securely — it is NOT echoed in the CFN console. (T-11-02)
  CodeBuildProject:
    Type: "AWS::CodeBuild::Project"
    Properties:
      ConcurrentBuildLimit: 1
      Artifacts:
        Type: NO_ARTIFACTS
      Environment:
        ComputeType: BUILD_GENERAL1_MEDIUM
        Image: "aws/codebuild/standard:7.0"
        # Required to run the Docker daemon: in `ecr` image-source mode the buildspec
        # builds the Use Case images from source and pushes them to the attendee's ECR
        # (`docker build`/`docker push`). The standard:7.0 image ships Docker; privileged
        # mode starts the daemon. Proven end-to-end in a dev-account CodeBuild build.
        PrivilegedMode: true
        Type: LINUX_CONTAINER
        EnvironmentVariables:
          - Name: STATE_BUCKET
            Type: PLAINTEXT
            Value: !Ref StateBucket
          - Name: ACME_EMAIL
            Type: PLAINTEXT
            Value: !Ref AcmeEmail
          - Name: ATTENDEE_ROLE_NAME
            Type: PLAINTEXT
            Value: !Ref AttendeeRoleName
          - Name: IMAGE_SOURCE
            Type: PLAINTEXT
            Value: !Ref ImageSource
          # Key id, not key material — used to write the full tier-2 state to S3
          # under SSE-KMS. Not sensitive.
          - Name: BUILD_SECRETS_KEY_ID
            Type: PLAINTEXT
            Value: !Ref BuildSecretsKey
          # SECRETS_MANAGER type, NOT PLAINTEXT: CodeBuild resolves the value at
          # build start and batch-get-projects returns only the secret ARN. The
          # PLAINTEXT type used above for ACME_EMAIL returns the value verbatim
          # to anyone who can describe the project.
          - Name: ICR_ENTITLEMENT_KEY
            Type: SECRETS_MANAGER
            Value: !Ref IcrEntitlementKeySecret
          - Name: VAULT_ENTERPRISE_LICENSE
            Type: SECRETS_MANAGER
            Value: !Ref VaultEnterpriseLicenseSecret
          - Name: IVIA_MMFA_PUSH_CLIENT_SECRET
            Type: SECRETS_MANAGER
            Value: !Ref IviaMmfaPushSecret
      ServiceRole: !GetAtt CodeBuildServiceRole.Arn
      Source:
        Type: S3
        Location: !Sub "${TerraformSourceBucket}/${AssetsKeyPrefix}"
        BuildSpec: buildspec/buildspec.yml
      # Tier 1 (~17 min) + tier 2 (~15 min) nominal, plus a worst case that adds
      # Step 7's 900s ACME gate and a repair pass. 60 was sized for tier 1 alone
      # and would now be reached on a merely slow run rather than a broken one.
      TimeoutInMinutes: 180
      LogsConfig:
        CloudWatchLogs:
          Status: ENABLED
          GroupName: "/aws/codebuild/workshop-tier1"

  # ─── IAM: Lambda trigger execution role ────────────────────────────────────
  # Lambda role for the Custom::CodeBuildTrigger function.
  # AWSLambdaBasicExecutionRole for CloudWatch Logs + StartBuild on THIS project only.
  # Threat T-11-06: StartBuild scoped to single project ARN (not "*").
  CodeBuildTriggerExecutionRole:
    Type: "AWS::IAM::Role"
    Properties:
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service:
                - lambda.amazonaws.com
            Action:
              - "sts:AssumeRole"
      ManagedPolicyArns:
        - "arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole"
      Policies:
        - PolicyName: RunCodeBuildProject
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Sid: RunCodeBuild
                Effect: Allow
                Action:
                  - "codebuild:StartBuild"
                Resource:
                  - !GetAtt CodeBuildProject.Arn

  # ─── Lambda: Custom::CodeBuildTrigger function ──────────────────────────────
  # Starts the CodeBuild project on CFN Create/Update/Delete.
  # Handler code copied verbatim from:
  # aws-samples/sample-iac-cloudformation-wrapper/terraform-project/cfn_bootstrap.yml
  # When CodeBuildCallback=true, injects CFN_EVENT_DATA + CFN_EVENT_RESPONSE_URL as
  # env var overrides so the buildspec finally block can PUT the callback response.
  CodeBuildTriggerFunction:
    Type: "AWS::Lambda::Function"
    Properties:
      Description: "Custom::CodeBuildTrigger — starts CodeBuild with CFN callback env vars injected"
      Handler: "index.lambda_handler"
      MemorySize: 128
      Role: !GetAtt CodeBuildTriggerExecutionRole.Arn
      Runtime: python3.13
      Timeout: 900
      Code:
        ZipFile: |
          # Copyright Amazon.com, Inc. or its affiliates. All Rights Reserved.
          """Custom CloudFormation Resource to kick off CodeBuild project builds

          CFn Properties
          --------------
          ProjectName : string
              Name of the AWS CodeBuild project to run
          BuildOnDelete : Optional[bool]
              Set `True` to also run a build on CloudFormation `Delete` events
          CodeBuildCallback : Optional[bool]
              Set `True` if your CodeBuild project handles the CFN CR callback,
              or leave default (False) to report success as soon as build starts.
          IgnoreUpdate : Optional[bool]
              Set `True` to only trigger a build on initial `Create` and ignore `Update`s

          CodeBuild Env Var Overrides (injected when CodeBuildCallback=true)
          ---------------------------
          CFN_EVENT_TYPE : Union["Create", "Update", "Delete"]
          CFN_EVENT_DATA : str
              JSON with LogicalResourceId, RequestId, StackId, NoEcho, Data.
              Your buildspec finally block adds Status, Reason, PhysicalResourceId
              and PUTs the whole object to CFN_EVENT_RESPONSE_URL.
          CFN_EVENT_RESPONSE_URL : str
              URL to PUT the CloudFormation response data to.
          """

          import json
          import logging
          import traceback

          import boto3
          import cfnresponse

          codebuild = boto3.client("codebuild")


          def _flag(value):
              # CloudFormation serialises custom-resource properties as STRINGS, so a
              # template `BuildOnDelete: false` arrives as the string "false" — which is
              # truthy in Python. Coerce explicitly so false flags stay false. Without this,
              # every stack rollback/delete fires a spurious build that re-runs the deploy.
              return str(value).strip().lower() in ("true", "1", "yes")


          def lambda_handler(event, context):
              try:
                  request_type = event["RequestType"]
                  if request_type == "Create":
                      handle_create(event, context)
                  elif request_type == "Update":
                      handle_update(event, context)
                  elif request_type == "Delete":
                      handle_delete(event, context)
                  else:
                      cfnresponse.send(
                          event, context, cfnresponse.FAILED, {},
                          reason=f"Unsupported CFN RequestType '{request_type}'",
                      )
              except Exception as e:
                  logging.error("Uncaught exception in CFN custom resource handler - reporting failure")
                  traceback.print_exc()
                  cfnresponse.send(
                      event, context, cfnresponse.FAILED, {},
                      reason=f"See CloudWatch {context.log_group_name} > {context.log_stream_name} - {str(e)}",
                  )
                  raise e


          def get_cfn_env_var_overrides(event):
              res_config = event.get("ResourceProperties", {})
              result = [
                  {"name": "CFN_EVENT_TYPE", "type": "PLAINTEXT", "value": event["RequestType"]},
              ]
              if _flag(res_config.get("CodeBuildCallback")):
                  result += [
                      {
                          "name": "CFN_EVENT_DATA",
                          "type": "PLAINTEXT",
                          "value": json.dumps({
                              "LogicalResourceId": event["LogicalResourceId"],
                              "PhysicalResourceId": event["LogicalResourceId"],
                              "RequestId": event["RequestId"],
                              "StackId": event["StackId"],
                              "NoEcho": event["ResourceProperties"].get("NoEcho", False),
                              "Data": {},
                          }),
                      },
                      {
                          "name": "CFN_EVENT_RESPONSE_URL",
                          "type": "PLAINTEXT",
                          "value": event["ResponseURL"],
                      },
                  ]
              return result


          def handle_create(event, context):
              logging.info("**Received create request - running CodeBuild job")
              res_config = event["ResourceProperties"]
              codebuild_env_vars = get_cfn_env_var_overrides(event)
              result = codebuild.start_build(
                  projectName=res_config["ProjectName"],
                  environmentVariablesOverride=codebuild_env_vars,
              )
              if not res_config.get("CodeBuildCallback"):
                  cfnresponse.send(
                      event, context, cfnresponse.SUCCESS,
                      {"Reason": f"Started CodeBuild #{result['build']['buildNumber']}"},
                      physicalResourceId=event["LogicalResourceId"],
                  )


          def handle_delete(event, context):
              logging.info("**Received delete event - no-op")
              res_config = event.get("ResourceProperties", {})
              build_on_delete = _flag(res_config.get("BuildOnDelete"))
              if build_on_delete:
                  codebuild_env_vars = get_cfn_env_var_overrides(event)
                  codebuild.start_build(
                      projectName=res_config["ProjectName"],
                      environmentVariablesOverride=codebuild_env_vars,
                  )
                  reason = "Deletion build initiated"
              else:
                  reason = "Delete is a no-op"
              if not (build_on_delete and _flag(res_config.get("CodeBuildCallback"))):
                  cfnresponse.send(
                      event, context, cfnresponse.SUCCESS, {"Reason": reason},
                      physicalResourceId=event["PhysicalResourceId"],
                  )


          def handle_update(event, context):
              logging.info("**Received update event")
              res_config = event.get("ResourceProperties", {})
              ignore_update = _flag(res_config.get("IgnoreUpdate"))
              if ignore_update:
                  reason = "Update is a no-op"
              else:
                  codebuild_env_vars = get_cfn_env_var_overrides(event)
                  result = codebuild.start_build(
                      projectName=res_config["ProjectName"],
                      environmentVariablesOverride=codebuild_env_vars,
                  )
                  if _flag(res_config.get("CodeBuildCallback")):
                      reason = None
                  else:
                      reason = f"Started CodeBuild #{result['build']['buildNumber']}"
              if reason is not None:
                  cfnresponse.send(
                      event, context, cfnresponse.SUCCESS, {"Reason": reason},
                      physicalResourceId=event["PhysicalResourceId"],
                  )

  # ─── Custom Resource: fire the CodeBuild tier-1 build on stack CREATE/DELETE ──
  # BuildOnDelete: true  → CFN Delete triggers a teardown build (the buildspec's Delete
  #   branch runs teardown.sh), so deleting the stack tears down tier-1 too — symmetric
  #   provision/deprovision via the stack. CFN waits for the finally callback; the 60-min
  #   CodeBuild timeout is the backstop so a stuck teardown can't block stack deletion forever.
  # CodeBuildCallback: true → CFN waits for the buildspec finally block to PUT response (D-13)
  # IgnoreUpdate: true → pinned deploy; don't re-run on parameter changes mid-event
  Tier1Deployment:
    Type: "Custom::CodeBuildTrigger"
    DependsOn:
      - CodeBuildProject
      - CodeBuildTriggerFunction
    Properties:
      ServiceToken: !GetAtt CodeBuildTriggerFunction.Arn
      ProjectName: !Ref CodeBuildProject
      BuildOnDelete: true
      CodeBuildCallback: true
      IgnoreUpdate: true

Outputs:
  CodeBuildProjectName:
    Description: "Name of the CodeBuild project that ran tier-1"
    Value: !Ref CodeBuildProject

  StateBucketName:
    Description: "S3 bucket holding the staged tier-1 Terraform state (attendees pull from here)"
    Value: !Ref StateBucket

  CodeBuildConsoleLink:
    Description: "Direct link to the CodeBuild project in the AWS console"
    Value: !Sub "https://${AWS::Region}.console.aws.amazon.com/codesuite/codebuild/${AWS::AccountId}/projects/${CodeBuildProject}"
