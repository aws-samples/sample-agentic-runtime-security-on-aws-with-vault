# Terraform
.terraform/
.terraform-build/
*.tfstate
*.tfstate.*
*.tfplan
crash.log
*.tfvars
*.tfvars.json
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Claude
.claude/


# Debug logs
logs/

# Python
.venv/
__pycache__/
*.pyc

# Workshop Studio
workshop/public/
# workshop/static/images/ is NO LONGER ignored: Workshop Studio publishes these
# files directly and cannot render .svg, so package-assets.sh rasterizes the
# dark-theme diagrams (assets/*.svg) to committed PNGs here at build time.
# Generated by package-assets.sh: full infrastructure/ tree synced for the CFN-wrapper
# CodeBuild source. Single source of truth is infrastructure/; this copy is regenerated
# and S3-synced to the WS assets bucket at publish time (never committed).
workshop/assets/terraform/

# Node
node_modules/

# reveal-md exports (any rendered slide-deck PDFs at repo root or below)
*.pdf
slides-site/
workshop/tmp/

# Instruqt distribution
# config.yml is a GENERATED artifact (see Decision 7 in
# .planning/phases/10-instruqt-port/10-01-PLAN.md). Tracked source is
# config.yml.tmpl + instruqt/scripts/render-config.sh; the rendered
# config.yml carries region literals and MUST NOT be committed.
instruqt/track/config.yml
