# vpc

Phase 2: VPC Stacks component (3 public + 3 private subnets in us-west-2, NAT gateway, S3 gateway endpoint, interface endpoints for bedrock-runtime, bedrock-agent-runtime, logs, sts, secretsmanager, kms).
