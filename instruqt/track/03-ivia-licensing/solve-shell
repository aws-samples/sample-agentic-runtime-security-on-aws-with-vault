#!/usr/bin/env bash
# 03-ivia-licensing/solve-shell
# Pure side-effect of track_scripts/setup-shell (org-secret injection) + the
# repo clone (cert ships in repo). No attendee action; solve is a no-op.
set -euo pipefail
exit 0
