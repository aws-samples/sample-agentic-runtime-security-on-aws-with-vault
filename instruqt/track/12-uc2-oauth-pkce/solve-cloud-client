#!/usr/bin/env bash
# 12-uc2-oauth-pkce/solve-cloud-client — tier-2 deploy puts the trust chain in place;
# no attendee action solves this beyond a successful login round-trip.
set -euo pipefail
exit 0
